<Window x:Class="FileSorterXT.Views.InputExtensionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Mapping" Height="210" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Enter an extension (example: .zip)" FontWeight="SemiBold" Margin="0,0,0,8"/>
    <TextBox x:Name="ExtBox" Grid.Row="1" Text=".zip"/>

    <TextBlock x:Name="ErrorText" Grid.Row="2" Foreground="DarkRed" TextWrapping="Wrap" Margin="0,0,0,8"/>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Click="Cancel_Click" MinWidth="120"/>
      <Button Content="Continue" Click="Ok_Click" MinWidth="120" Margin="8,4,0,10"/>
    </StackPanel>
  </Grid>
</Window>
